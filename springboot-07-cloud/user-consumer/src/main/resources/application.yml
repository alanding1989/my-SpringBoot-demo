spring:
    application:
        name: consumer-user
server:
    port: 8200

eureka:
    instance:
        # 注册服务的时候使用服务的Ip地址
        prefer-ip-address: true
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka/
